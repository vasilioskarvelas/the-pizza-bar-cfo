{
  "name": "AlertRule",
  "type": "object",
  "properties": {
    "organisation_id": {
      "type": "string"
    },
    "rule_key": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "severity": {
      "type": "string",
      "enum": [
        "critical",
        "high",
        "medium",
        "low"
      ],
      "default": "medium"
    },
    "affected_metric": {
      "type": "string"
    },
    "description": {
      "type": "string"
    },
    "threshold": {
      "type": "number"
    },
    "comparator": {
      "type": "string",
      "enum": [
        "lt",
        "lte",
        "gt",
        "gte",
        "eq"
      ],
      "default": "lt"
    },
    "business_impact": {
      "type": "string"
    },
    "recommended_action": {
      "type": "string"
    },
    "enabled": {
      "type": "boolean",
      "default": true
    },
    "sort_order": {
      "type": "number",
      "default": 0
    }
  },
  "required": [
    "organisation_id",
    "rule_key",
    "title",
    "severity"
  ],
  "rls": {
    "read": {
      "$and": [
        {
          "data.organisation_id": "{{user.data.organisation_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "create": {
      "$and": [
        {
          "data.organisation_id": "{{user.data.organisation_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$and": [
        {
          "data.organisation_id": "{{user.data.organisation_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$and": [
        {
          "data.organisation_id": "{{user.data.organisation_id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}